# UniGather v0.0.4

## 发布内容

- 新增独立“邮件发送”工作台：扫描本地材料目录，按通讯录单位名称和别名匹配附件，逐单位点对点发送；支持测试发送、正式确认、发送进度、历史记录与失败重试。
- 通讯录替代原“分子公司”模块：支持联系人、邮箱、电话、单位别名、多邮箱导入，以及新增、编辑、删除联系人。
- 邮箱配置增加 SMTP 发信连接和收发双连接测试；代理配置由收件和发件共同使用，密码继续保存在 Windows 凭据管理器。
- 收集任务支持单位范围、任务起始/截止时间、立即刷新、自动轮询、任务详情、待反馈单位导出与邮件匹配结果查看。
- 收件箱优化为固定双栏阅读布局，列表和正文独立滚动，避免长邮件造成横向溢出。
- 收集任务匹配邮件支持分页（10/20/31/40/50 条）、已反馈/待反馈/待确认/未匹配下钻表格，以及“已识别单位 + 未匹配原因”展示。
- 邮件正文采用仿宋 GB2312 14 号字体、默认签名采用微软雅黑 9 号字体；无附件邮件会将正文生成 Word 文档后归档。
- 修复任务页每 30 秒整页重绘问题，定时轮询现在只更新收件倒计时，并在到期时执行后台收件。

## 匹配与进度说明

- 邮件匹配进度按“已完成匹配判断邮件数 / 任务时间范围内邮件总数”计算；已匹配、待确认、未匹配邮件均计入已判断。
- 单位反馈完成度按“已确认反馈单位数 / 任务单位总数”单独显示，用于催办。
- 发件人邮箱精确匹配优先；邮箱未知时会尝试从发件人显示名称匹配单位或单位别名。若主题或正文规则不符合，仍会显示已识别单位和未匹配原因，但不会计入已反馈。

## 已知限制

- 更新安装需要退出当前软件后运行下载的 NSIS 或 MSI 安装包，不执行静默自更新。
- 扫描件或图片附件仅归档，不进行 OCR。
- 应用彻底退出后停止自动收件；不接入 OA、SSO 或 Exchange API。

## 验证

- 前端核心逻辑测试：47 项通过。
- Rust 单元测试：26 项通过。
- Vite 生产构建、Windows Tauri release 构建通过。

## 安装包

- `UniGather_0.0.4_x64-setup.exe`：NSIS 安装包，适合普通用户。
- `UniGather_0.0.4_x64_zh-CN.msi`：MSI 安装包，适合企业软件分发。
