# UniGather v0.0.2

## 发布内容

- 基于 v0.0.1 的稳定更新，版本号统一为 `0.0.2`。
- “立即收件”改为后台逐封收取：窗口保持可操作，页面显示当前阶段、已处理数量和新增数量，收到一封即刷新一封。
- IMAP 按 UID 逐封读取，POP3 按邮件逐封读取；支持 SSL/TLS、HTTP CONNECT、SOCKS5/SOCKS5H 代理。
- 收件错误、认证失败和代理连接失败会在页面显示，不再用浏览器预览模式提示掩盖桌面端错误。
- 收件箱附件支持点击打开本地归档文件；附件仍按本地路径归档，不会上传至 AI 服务。
- 继续支持任务起始时间补查、单位 Excel/CSV 导入、单位联系人编辑、AI 配置、材料保存路径和任务详情编辑。
- 修订邮件收取设计文档和使用说明，补充当前命令、进度刷新和附件行为。

## 已知限制

- 扫描件或图片附件仅归档，不进行 OCR。
- 文本附件内容提取和自动匹配仍需后续增强。
- 应用彻底退出后停止收件；不接入 OA、SSO 或 Exchange API。

## 验证

- 前端核心逻辑测试：21 项通过。
- Rust 单元测试：2 项通过。
- Vite 生产构建、Windows Tauri release 构建通过。

## 安装包

- `UniGather_0.0.2_x64-setup.exe`：NSIS 安装包。
- `UniGather_0.0.2_x64_zh-CN.msi`：MSI 安装包。
