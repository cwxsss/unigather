# UniGather v0.0.3

## 发布内容

- 重新设计“检查软件更新”流程，不再使用在 Tauri 窗口中无响应的外链按钮。
- 应用内显示连接、下载中、下载百分比、下载完成和下载失败状态。
- 下载完成后自动保存安装包到 Windows“下载”文件夹，并提示退出当前版本后运行安装程序。
- 如果网络策略阻止应用内下载，会自动尝试打开浏览器下载地址，并显示回退提示。
- 增加 GitHub 下载进度格式化和更新流程回归测试。
- 保留 v0.0.2 的后台逐封收件、收件进度、代理收件、附件打开和本地归档能力。

## 已知限制

- 更新安装需要退出当前软件后运行下载的 NSIS 或 MSI 安装包，不执行静默自更新。
- 扫描件或图片附件仅归档，不进行 OCR。
- 应用彻底退出后停止收件；不接入 OA、SSO 或 Exchange API。

## 验证

- 前端核心逻辑测试：22 项通过。
- Rust 单元测试：2 项通过。
- Vite 生产构建、Windows Tauri release 构建通过。

## 安装包

- `UniGather_0.0.3_x64-setup.exe`：NSIS 安装包。
- `UniGather_0.0.3_x64_zh-CN.msi`：MSI 安装包。
