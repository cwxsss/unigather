# UniGather v0.0.1

## 发布内容

- 首个 Windows 单机版 UniGather 材料收集系统。
- 深色科技感中文桌面界面：仪表盘、任务中心、单位管理、邮箱配置、材料归档和系统设置。
- 任务创建、列表刷新、删除任务、任务详情/编辑和空状态展示。
- 单位 CSV/XLS/XLSX 导入（单位、姓名、多个邮箱）、模板下载和导入后单行编辑。
- Foxmail 风格收件箱，支持邮件搜索、排序、详情查看和附件打开。
- 邮箱配置表单重做：IMAP/POP3、端口联动、密码显示/隐藏、代理设置和连接配置校验。
- 支持真实 IMAP/POP3 收件、SSL/TLS、HTTP CONNECT、SOCKS5/SOCKS5H 代理和 Windows 凭据管理器。
- “立即收件”改为后台逐封收取，显示实时进度，收件箱同步刷新，不再阻塞窗口。
- 材料归档路径可配置，附件重名自动追加序号，不覆盖已有文件。
- 单位匹配、任务起始时间补查、AI 配置和本地设置保存。
- 修复 Windows 发布版启动时显示黑色控制台窗口的问题，发布版改为 GUI 子系统启动。
- 系统设置增加 GitHub Release 更新检查和新版安装包下载入口。
- 前端核心逻辑测试 21 项、Rust 单元测试 2 项全部通过；Rust `cargo check` 和 Windows 生产构建通过。

## 已知限制

扫描件或图片附件仍只归档，不进行 OCR；文本附件内容提取和自动匹配仍需后续增强。当前版本支持真实收件、附件本地归档与本地打开。

## 安装包

- `UniGather_0.0.1_x64-setup.exe`：NSIS 安装包。
- `UniGather_0.0.1_x64_zh-CN.msi`：MSI 安装包。
